const path = require("path");

module.exports = {
    config: path.resolve("database", "config.js"), //configurações para acessar o banco
    "migrations-path": path.resolve("database", "migrations"),
    "seeders-path": path.resolve("database", "seeders"),
    "models-path": path.resolve("models")  //manipular o banco...insert, update.. etc
}